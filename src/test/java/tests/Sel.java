package test.java.tests;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;
import org.testng.annotations.AfterMethod;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.Test;

import java.util.concurrent.TimeUnit;

public class Sel {
/*


    @Test
    public void test1() throws InterruptedException {
        driver.get("https://legacy.gitbook.com/book/kreisfahrer/selenium-webdriver/details");
        WebElement blog = (new WebDriverWait(driver, 10)).until(ExpectedConditions.presenceOfElementLocated(By.xpath("(//a[@href='/blog'])[1]")));
        blog.click();
        WebDriverWait wait = new WebDriverWait(driver, 10);
        wait.until(ExpectedConditions.and(
                ExpectedConditions.presenceOfElementLocated(By.xpath("//a[text()='All posts']"))
        ));
        Thread.sleep(5000);
    }

    @Test
    public void test2() throws InterruptedException {
        //driver.manage().timeouts().implicitlyWait(10, TimeUnit.SECONDS);
        driver.get("https://legacy.gitbook.com/book/kreisfahrer/selenium-webdriver/details");
        WebElement blog = (new WebDriverWait(driver, 10)).until(ExpectedConditions.presenceOfElementLocated(By.xpath("(//a[@href='/blog'])[1]")));
        blog.click();
        WebDriverWait wait = new WebDriverWait(driver, 10);
        wait.until(ExpectedConditions.and(
                ExpectedConditions.presenceOfElementLocated(By.xpath("//a[text()='All posts']"))
        ));
        Thread.sleep(5000);
    }

*/

}
